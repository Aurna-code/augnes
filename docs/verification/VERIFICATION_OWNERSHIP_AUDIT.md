# Verification responsibility audit

Audit baseline: `22c0433d245da7e9357e7f6a50c7e6e0f31902cb` in
`hynk-studio/augnes`. This is supporting verification analysis, not a product,
protocol, research-sequencing, or execution-authority owner. Final exact-head
execution evidence belongs in the accompanying Draft PR and local receipt.

## Finding and bounded change

Most expensive coverage inspected protects current, distinct responsibilities.
The justified change is a missing exact owner registration for the seven-file
project-experience Browser verification family. Its existing callers are unit,
authority, and its own Browser phase. Previously these files selected
`full-canonical` through `unknown_or_unmatched_owner`, even for the four-file
hydration correction in [PR #1200](https://github.com/hynk-studio/augnes/pull/1200).

The new owner selects the existing exact-change validator, root and nested
`npm ci`, typecheck, unit, authority, and `e2e-project-experience`. Planner tests
are extended; no existing product coverage is removed or weakened. Package
commands, timeouts, full phases, product code, and research registrations are
unchanged. This reduces unrelated orchestration;
it does not claim that every remaining aggregate is minimal.

## Inspected owners and method

Inspection traced package commands into `run-canonical-test-suite.mjs`, its
integration groups and operability shard registrations, protocol conformance
imports, Browser manifest executables and result-contract consumers, the change
planner and owner manifest, Local Canonical executor/environment/receipt
contracts, the active roadmap, and
[retention/reduction policy](../REPOSITORY_REDUCTION_SCOPE.md).

Representative command-backed consumer searches:

```bash
rg -n 'browser-validate-project-experience-v1|project-experience-result-contract-v1|project-experience-browser-fixture-v1|project-experience-hydration-boundary-v1' scripts package.json
rg -n 'operational-reentry|governed-actor|model-host-succession' lib/vnext/model-gateway scripts/run-canonical-test-suite.mjs scripts/vnext-protocol-conformance.ts
rg -n 'INCLUDE_SR3|operator-adapter-only' scripts/test-local-project-verification-adapter.ts scripts/test-vnext-project-verify-lifecycle.ts
rg -n 'INTENTIONALLY_REPEATED|RESPONSIBILITY_CONTRACTS' scripts/runtime-operability-ownership.mjs
```

An independent reviewer traced research registrations and apparent duplicated
coverage. That review found the authority-suite static consumer of the Browser
executable; the proposed narrow lane was expanded to retain it before deciding
verification. Static reachability alone was not treated as retirement proof.

## Responsibility classification

Classes describe responsibilities, not blanket directory dispositions. For
`HIGH_RISK_TARGETED`, the current sufficient surface often remains Full
Canonical because a smaller complete owner graph has not been proved. A
qualification or research label below does not silently remove a registered
regression test from production verification.

| Verification family and actual consumer | Classification | Gate/retention decision |
|---|---|---|
| Exact repository/origin/base/head, clean tree before/after, pinned environment, receipt integrity/staleness, selected inventory and cleanup | KEEP_ALWAYS | Executor and receipt admission remain universal for deciding runs; never reuse evidence across heads/environments. |
| Markdown/link/private-path checks and root `AGENTS.md` operating-policy contracts | OWNER_TARGETED | Preserve existing docs and exact single-file policy lanes. Mixed policy changes still broaden. |
| Typecheck and deterministic protocol primitives/conformance | OWNER_TARGETED | Required by affected source owners; complete Core/semantic changes retain broader coverage. Unit is an aggregate, not a claim of universal cheapness. |
| Proposal, ReviewDecision, decision, Transition, durable semantic loop and Verify lifecycle/admission/replay | HIGH_RISK_TARGETED | Keep protocol, authority, integration, relevant Browser and recovery coverage. Source-linked lineage and applied-state distinctions are separate assertions. |
| Project/GuideBrief/presentation and Browser immutable fixture/result contracts | OWNER_TARGETED | Keep focused commands and affected detailed Browser owner; new exact project verification lane retains unit and authority consumers. |
| Context attribution, shadow navigation, dynamics, friction proposals and continuation | HIGH_RISK_TARGETED | Research-derived names conceal current admission, policy, Inspector, project-continuity and portability consumers; retain regression gating. |
| Deterministic model/host succession and re-entry perturbation fixture/report regressions | OWNER_TARGETED | Retained toolbox contracts build real admission/lineage fixtures and check source binding, capability narrowing and inconclusive report semantics. They still run in the coarse unit aggregate; scientific outcomes remain historical. |
| Codex registry, production resolution, managed store, sandbox, isolated auth and candidate/nonpromotion contracts | HIGH_RISK_TARGETED | Keep current high-risk precedence. Shared native-host source still broadens; isolated registered test entry points already target typecheck/integration. |
| Real Codex binary/package/platform compatibility observations and release qualification | QUALIFICATION_ONLY | Keep separately invoked qualification; observations do not promote production or replace synthetic safety regressions. No real candidate/provider qualification launched by this audit. |
| Model Gateway, provider adapters, credential containment, egress and authority | HIGH_RISK_TARGETED | Current Gateway and Responses adapter import v0.1–v0.4/cross-case codecs. Fake-transport tests retain current correlation, envelope, authorization and receipt invariants. |
| ACGC cohort/probe authorization, schema, terminalization, append-only evidence and codec contract tests | HIGH_RISK_TARGETED | Keep registered zero-egress authority tests despite generation labels; their scientific outputs have separate status. |
| DB schema, migration, recovery validator, backup/atomic restore, replay and production reader fidelity | HIGH_RISK_TARGETED | Keep isolated integration and operability matrices, including malformed material, crash and rollback cases. |
| Supervisor, Companion, process lifecycle, cancellation, restart/reconciliation, poisoned environment and ownership refusal | HIGH_RISK_TARGETED | Preserve shard-owned resources, natural exit, stream closure and zero residue. Repeated isolation preconditions are intentional. |
| Distributable package regression and packaged-runtime update/restore | HIGH_RISK_TARGETED | Current preflight bypass, invalid import, v1 migration, supervisor replacement, timeout, rollback and hard-crash tests protect runtime/data boundaries. |
| Platform/release certification beyond existing regression contracts | QUALIFICATION_ONLY | Remains explicit qualification; whole package/operability suites cannot be reclassified this way. |
| Project-experience detailed Browser | OWNER_TARGETED | Owns onboarding/naming, home/shell, locked/retired routes and responsive presentation in private resources. |
| Operator review/control, native execution, multi-candidate and continuity Browser | HIGH_RISK_TARGETED | Preserve authentication, approval/cancel/resume, exact candidate/Transition targeting, portability, restart and lineage. |
| Cross-boundary golden | HIGH_RISK_TARGETED | Keep connection → work → explicit deterministic Start → receipt → proposal composition. It has no detailed matrices to deduplicate. |
| Root/nested dependency preparation | KEEP_ALWAYS | For source-bearing deciding plans, retain clean `npm ci` and lock provenance. Existing docs/policy lanes remain dependency-free. Installed trees/cache never become authority. |
| Production build | HIGH_RISK_TARGETED | Retain on package/build/shared composition and Full Canonical. Harness-only source has no production build consumer. |
| RW1 benefit pilot and pre-action overlay, with current roadmap collection owner #1130 | RESEARCH_ACTIVE | Standalone tests/CLIs remain runnable and absent from default Canonical registrations. Research execution still needs its own authority. |
| Frozen benchmark/cohort scientific outcomes, stopped P6 lanes and terminal closeouts | HISTORICAL_OR_SUPERSEDED | Preserve exact inconclusive/blocked/terminal meaning; current codec, privacy, archive-isolation and admission tests remain separate kept responsibilities. |
| Apparent obsolete/duplicate wrappers and probes without complete disposition proof | RETIRE_CANDIDATE | No removal admitted. Current or unknown consumers block retirement; classification alone supplies no deletion authority. |

## Concrete consumer and disposition proof

Exact changed registration scope (all regular files, deletion policy `full`):

| Admitted path in `scripts/` | Current consumers and replacement proof surface |
|---|---|
| `browser-validate-project-experience-v1.mjs` | Canonical `projectExperienceStep`; Browser manifest; unit's operator-pure and owner-manifest contracts; authority's verification-policy contract. Run the same Browser child, unit and authority. |
| `project-experience-browser-fixture-v1.ts` | Project Browser executable and its unit fixture test. Immutable fixture copied into a private writable DB; production persistence imports are inputs, not reverse consumers. |
| `test-project-experience-browser-fixture-v1.ts` | Root package focused command and Canonical unit registration. Keep that exact child. |
| `project-experience-result-contract-v1.mjs` | Project Browser executable, unit result test, unit Browser-manifest contract. Keep all callers. |
| `test-project-experience-result-contract-v1.mjs` | Root focused command and unit registration; imports the hydration regression test. |
| `project-experience-hydration-boundary-v1.mjs` | Project Browser executable and hydration regression test. No product import. |
| `test-project-experience-hydration-boundary-v1.mjs` | Imported by the registered result-contract test, so unit executes it. |

The active semantic owner remains the
[Browser verification contract](../CANONICAL_BROWSER_VERIFICATION_OWNERSHIP_V1.md);
the Local Canonical change manifest owns plan admission. Package commands,
string-addressed suite registration, manifest executable references, static
source-reading tests and module imports were checked. No executable is moved
or removed, so existing manual/external invocations remain available.

Data proof: no schema, production reader/writer, migration, export/import,
backup, restore, replay or user data changes. These fixture writers still use
their existing isolated resources. Authority/safety proof: no semantic,
provider, external-effect, publication or merge grant changes; receipt,
credential, process, cancellation, cleanup and recovery assertions remain in
their unchanged owners. Candidate/review/research output gains no authority.

Replacement parity: only unrelated outer phases are omitted for these seven
paths. Every identified consumer runs in typecheck/unit/authority/its Browser
phase. Shared fixture builder, process lifecycle, runtime or owner-manifest
changes are outside this admission and still escalate. Rollback: revert the
owner registration to the audit base, or use explicit `verify:local:full`
immediately; no data migration or repair is required. Unknown future consumers
require revisiting this exact mapping, not widening it by filename resemblance.
The new `literal_exact_paths` rule compares raw Git paths without case folding
or path normalization aliases. Existing broader high-risk rules retain their
conservative case-insensitive matching and precedence. An independent review
identified the need for this distinction on case-sensitive filesystems.

## Suspicions resolved or left open

- **Incomplete coverage:** confirmed for this exact harness family. Fixed by
  owner mapping, not removing tests.
- **Native-host mappings shadowed:** confirmed: high-risk matching precedes
  targeted matching, so several native-host source entries cannot select their
  nominal narrow owner. Their registered test-only entry points can. Do not
  reverse precedence globally: adapter, credential, selection and managed-store
  consumers span integration, operability and Browser execution.
- **Research mixed with production:** labels are mixed; responsibilities are
  not interchangeable. Gateway/adapter imports and persisted continuation
  consumers require retention. Standalone live research is already separate.
- **Literal duplication:** standalone strategy-composition wrappers invoke the
  same conformance functions, but are not separately in the default suite.
  `--operator-adapter-only` is disjoint from default Verify lifecycle;
  `--sr3-lifecycle` adds a production chain while the baseline adapter mode owns
  a distinct criterion-isolation branch. No entire equivalent execution was
  found safe to absorb.
- **Browser/golden overlap:** manifest contracts partition 69 project,
  136 operator and 29 continuity detailed fields. Golden has zero detailed
  fields and explicitly forbids those matrices. It remains composition proof.
- **Operability overlap:** the responsibility owner explicitly repeats runtime,
  MCP, isolated resources, zero egress and natural cleanup per independently
  owned child. Those are required preconditions, not duplicate success cases.
- **Framework recursion:** planner/executor/owner-manifest changes still force
  broad verification. This PR deliberately retains that self-change fence.
- **Dependency/build cost:** both installations together measured 7.095s in
  PR #1207 versus 1,784.955s for integration plus operability. Avoiding installs
  would trade provenance for little savings. Build remains unchanged in Full.

## Target model and remaining granularity

Use existing architecture layers: (1) universal exact provenance/admission;
(2) owner-targeted static/correctness plus detailed Browser proof;
(3) high-risk boundary composition; (4) separately authorized runtime/platform
qualification; (5) research/evaluation commands; (6) explicit complete-system
Canonical. Do not add a new parallel runner or infer ownership from names.

The `changed` mode's `full-canonical` label can still select a Browser subset
through the pre-existing Browser map. Explicit `full` always forces every
permanent Browser phase. The examples below show actual phase inventories,
rather than assuming that the plan label always means fourteen phases.

Unit and authority are still coarse aggregates. Extracting a smaller static
Browser-policy phase, defining closed runtime ownership, and separating
report-only research from shared codecs are future candidates. They need their
own registration/consumer/replacement proof. This change does not declare all
unmatched paths intrinsically cross-cutting.

## Before/after planner validation

Reproducible, exact synthetic Git fixture comparisons:

```bash
node scripts/test-canonical-change-planner.mjs --compare-base 22c0433d245da7e9357e7f6a50c7e6e0f31902cb
```

The optional comparison snapshots the old planner and its two manifests from
the exact base; both planners receive the same fixture base/head commits.
Output includes their SHAs, owners, reasons and expanded phase arrays.
Temporary fixture repositories and planner snapshots are cleaned in `finally`.
The normal registered test needs no historical source and keeps its 60s bound.

Phase abbreviations: `D` = documentation validator; `P` = operating-policy
validator + planner/executor/receipt/verification contract tests;
`N` = root + nested clean installs; `T` = typecheck; `U` = unit; `A` = authority;
`V` = exact targeted validator; `B` = build; `I` = integration; `O` = operability;
`E-project`, `E-native`, `E-multi` = those detailed Browser phases;
`E-all` = project, review/control, native, multi-candidate, continuity, golden.
`F` = `N,T,B,U,A,I,O` in that order. All rows are old → new on macOS.

| Exact fixture class/path | Old plan/phases | New plan/phases | Sufficiency and escalation |
|---|---|---|---|
| Docs: `docs/guide.md` | documentation-only: D | same | Prose/link/private-path validator; non-doc, unsafe mode or deletion broadens. |
| Root policy: `AGENTS.md` | operating-policy-only: P | same | Existing policy contracts; any second path, nested policy, rename or deletion broadens. |
| UI: `components/guide-brief/current-action.tsx` | owner-targeted: V,N,T,U,E-multi | same | Existing single Browser owner; second/shared owner broadens. |
| Core: `lib/vnext/persistence/durable-semantic-store.ts` | full-canonical: F,E-all | same | Unknown Browser ownership fails closed; no demotion of semantic/data scope. |
| DB: `data/migrations/001.sql` | full-canonical: F,E-all | same | Schema/migration owner preserves replay/restore; already broad. |
| Registry: `lib/vnext/native-host/codex-qualified-runtime-registry.v1.json` | full-canonical: F,E-native | same | Shared native-host precedence; unknown/shared Browser companion path expands E-all. |
| Managed: `lib/vnext/native-host/codex-managed-runtime-store.ts` | full-canonical: F,E-native | same | Current runtime/recovery consumers; broad composition remains. |
| Browser executable: `scripts/browser-validate-project-experience-v1.mjs` | full-canonical: F,E-project | owner-targeted: V,N,T,U,A,E-project | All actual harness consumers retained; shared/new helper or another Browser owner broadens. |
| Seven-file Browser family + docs | full-canonical: F,E-all | owner-targeted: V,N,T,U,A,E-project | Exact allowlist and prose; deletion/rename/executable mode/shared source broadens. |
| Framework: `scripts/canonical-change-planner.mjs` | full-canonical: F,E-all | same | Self-change fence retained, including owner manifest/executor/receipt/runner. |
| Package or nested lockfile | full-canonical: F,E-all | same | Install/build/native ABI/distribution/runtime composition retained. |
| Research-only script: `scripts/operational-reentry-perturbation-report.ts` | full-canonical: F,E-all | same | Unmatched executable remains fail-closed; no complete narrower ownership proved. |
| Unknown: `docs/unknown.payload` | full-canonical: F,E-all | same | Unknown responsibility cannot authorize a smaller gate. |

Additional negative cases cover each admitted file, complete-family edits,
same-owner product composition, a second Browser owner, shared fixture builder,
process lifecycle, both manifests, runner, executor, receipt, native source,
Core/DB changes, addition, deletion, rename, executable mode, case variants,
backslash aliases and unmatched lookalike helper. Literal manifest entries must
be normalized; overlapping literal/legacy exact registrations are rejected.

## Measured cost and bounded estimates

Read merged PR bodies and local per-phase receipts/logs. These historical
measurements are not deciding evidence for this new head. No raw receipt or
log is committed or uploaded. Milliseconds below are observed, not invented.

| Recent merged PR | Measured full wall time | New selection replay / estimated effect |
|---|---:|---|
| [#1200](https://github.com/hynk-studio/augnes/pull/1200), hydration harness correction | 3,032,660 ms (50m33s) | Exact old base `ee0617e31210ef29d7cf043bcc48e4fd45ae3183`, head `8434f6cc13a9b7e860755fe92ee6b8f61a856ac7`: full → seven-phase project verification owner. |
| [#1205](https://github.com/hynk-studio/augnes/pull/1205), Codex candidate/registry | 3,178,768 ms (52m59s) | Exact historical diff remains full with all Browser phases; no estimated savings. |
| [#1207](https://github.com/hynk-studio/augnes/pull/1207), rolling candidate/credential binding | 3,141,343 ms (52m21s) | Exact historical diff remains full with all Browser phases; no estimated savings. |

PR #1200 retained-phase measurements: installs 5,129 + 1,627; typecheck 27,021;
unit 284,286; authority 125,784; project Browser 163,874 = **607,721 ms**.
Its full phase sum was 3,024,571 ms, leaving 8,089 ms of observed outer overhead.
Keeping that overhead gives an estimate of **615,810 ms (10m16s), plus the new
validator's unmeasured cost**, compared with the measured 50m33s. This is a
phase-sum estimate, about 40 minutes/80% lower for this specific change class;
it is not a measured new deciding run or a guarantee about future PRs.

PR #1207 phase measurements: installs 7,095; typecheck 26,434; build 38,241;
unit 270,922; authority 122,044; integration 755,729; operability 1,029,226;
Browser phases 165,958 / 307,379 / 146,078 / 67,661 / 152,017 / 44,017 ms.
Its local logs identify reconstruction conformance at 552,890 ms, packaged
runtime at 331,432 ms and crash/reconciliation at 238,448 ms. Those costs have
current correctness owners and are intentionally retained for relevant changes.

## Conservative verification and risks

This PR changes verification ownership, so its own deciding proof must remain
explicit Full Canonical, with the pre-existing full executor/suite/phase,
dependency, Browser, environment, lifecycle and receipt source unchanged.
Compare those source inventories against the exact audit base before running;
do not accept this modified manifest's narrow self-assessment. Run focused
planner/contract/executor/receipt checks during development, independently
review the final diff for missing consumers, then commit a clean exact head,
run `verify:local:full -- --base <base> --head <head>` once and validate the
receipt. The PR records actual results, exact identity, commands and cleanup.

Remaining risks: future reverse consumers can outgrow this exact mapping;
unit/authority remain coarse; runtime-owner shadowing and unknown research
paths still overselect; platform/browser/environment timing varies. The optional
comparison reuses the unchanged phase executor to expand phase names and is
diagnostic, not independent attestation. No live provider/candidate/research,
hosted CI, release/platform certification, deployment or publication is implied.
