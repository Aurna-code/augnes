# P5.1a executed work to reviewed follow-up

Issue #1230, under #1215 / P5.1 and coordination #1209. P1 #1210 and
bounded P1.5 #1228 remain completed. This slice adds regression evidence to
existing owners; it adds no production behavior, durable format or migration.

## Source audit and selected path

The audited base is `deb5deeea04f01ea6fa12623227f395bf708f3a5`, tree
`6f6c541433cfff0fedb5699da6915d3d35b886ec`, in `hynk-studio/augnes`.
Current repository identity and exact main were checked before branching.
No equivalent active branch or PR was found. Final clean base/head, environment,
planner selection and deciding receipt belong to the PR verification closeout.

The current-continuity contract resolved the exact live Companion and project:
`no_current_work`, `no_run`, `no_result`, `no_proposal`. These are current exact
absence results. `CODEX_WORK_ID` was absent; no development-task work/run/proof
identity was created. All scenario identities come from disposable test owners.

| Boundary and normal entry | Producer and persistence / authority owner | Consumer and existing evidence | Gap covered here |
|---|---|---|---|
| Project Home: define work, revise selected source notes, compare and save | `project-work-initialization.ts`, `project-work-revision.ts`; authenticated project-continuity route; immutable packet and local session | Persisted native-host admission; project-work initialization tests and native-host Browser first Start / P1.5 cases | Start with pre-result material only; execution must keep the revision editor closed |
| Explicit deterministic host action / managed Start | `direct-native-host-round-trip.ts` and normal native-host result persistence; execution remains a separate action | `run-assessment-proposal-admission.ts`; Project Home latest-result link and result-review page | Actually execute X, persist its result and automatically produce its source-bound proposal |
| Result → proposal → operation-aware revision form | `operator-pilot-proposal-revision.ts`; immutable revised proposal retaining packet, receipt, assessment and candidate bindings | Semantic review; existing review-control Browser owner covers form and decision mechanics | Enter NEW correction after the actual result, with original observation and assessment preserved |
| Save eligible review decision, review impact, confirm, apply | `operator-pilot-review-material.ts`, `operator-pilot-semantic-transition.ts`; separate Decision, gate and atomic Transition / semantic state | `persisted-semantic-context-compiler.ts` called by the authenticated semantic-transition route | Apply only the reviewed validation change and compile its actual later packet |
| Project Home next explicit execution | Fresh continuity projection and `admitPersistedHostTaskContextPacketV01`; exact project, source and currentness validation | Production native request builder and normal persisted result | Reopen the database / restart the disposable service; observe corrected fresh preparation and a distinct request/result |

Owner paths above are under `lib/vnext/runtime/`; entry UI is under
`components/workbench/semantic-review/` and routes under
`app/api/vnext/operator/`. The new assertions live in the existing
`scripts/test-vnext-project-work-initialization.ts` and
`scripts/browser-validate-operator-native-host-execution-v1.mjs` owners.

The operational alternative was inspected but is not claimed. Its compiler
requires the specific prior packet, source Transition, ContextUseReview and
friction/decision chain; the source-built semantic seed used by its admission
tests does not prove arbitrary post-result notes enter through a normal UI.
P1.5 lookup remains restricted to eligible pre-execution revisions. Neither
alternative is widened to make this scenario pass.

## Fixed scenario, cutoff and meaning

Before execution, the disposable project contains a goal about A under X/Y and
two explicitly selected notes: the bench protocol and an unverified possible
global-failure explanation. Input files contain X measured 7 against limit 5,
Y without a reading, and separate calibration B reference/expected values of 5.
The expected correction and next-check meaning are fixed in the test before the
first invocation, but no correction, outcome proposal or later packet is seeded
into product state.

The deterministic adapter actually reads and compares X. Normal persistence
records the completed execution, conditional failed check, untested Y and
unknown cause. The normal assessment produces an initially ineligible
`validation_delta` candidate with task success still `unknown`.

Only after that result, the user-authored revision says: X exceeded the limit
only in the cold-start sample; reject “A always fails” while retaining the
reading; Y is untested, not prohibited; the cause remains uncertain; defer Y
until calibration B is checked, then revisit; next compare B with its expected
value. It is a `user_declaration` linked to the original result/proposal, not a
new observation or automatic acceptance. Observations, attestations, inferences
and source assessment remain byte-equivalent in the revised proposal.

The explicit Decision accepts this eligible validation change. Independent
preview writes nothing; confirmation records only its gate; apply atomically
persists the intended semantic effect, Transition and compiler-produced packet.
The accepted state is the bounded correction including its uncertainty and
defer condition. This does not fabricate an accepted Y result. A standalone
`defer` ReviewDecision progressing without a Transition is not claimed here.

After closing and reopening the file-backed database, production continuity
selects the new packet and normal admission checks it. The next request retains
the same task and work reference according to the existing semantic compiler
contract, while packet, run and request identities differ. Original selected
notes remain separately typed; the accepted correction occurs exactly once
with Transition provenance. The next deterministic consumer requires that
correction, reads calibration B and compares 5 with 5. It persists a second
ordinary result; it does not run Y. No expected packet is supplied to the reader
or copied into the successor.

## Browser connection and practical actions

The existing native-host Browser owner first proves selected-note revision /
recall and first Start, including stale-editor invalidation. Its new phase uses
the existing primary project's just-completed managed native-host result from
the fake native server. The result and its proposal are produced during the run,
not supplied as the post-result fixture. The managed approval protocol uses a
deterministic fake server; “live” in existing test names does not mean a provider
or model was contacted.

The normal path is Project Home **Review result → review proposal → revise
candidate → save review → review impact → confirm → apply → Project Home →
project options → deterministic host action**. The correction restricts what
the bounded host-delivery check establishes, preserves uncertainty and defers
Y pending evidence. A restart through the existing lifecycle owner preserves
the Browser session, reconstructs production preparation, and the next explicit
action creates a fresh persisted native-host result on that packet.

This Browser delivery scenario proves the user connection to the same owners;
the file-backed X/B scenario separately proves a meaningful deterministic next
check. Neither claims model interpretation or scientific utility. The Browser
path uses 11 button/link/checkbox activations, five form-field changes and one
return to Project Home after the initial result. Authentication/project setup
and the first execution precede those actions. Internal test-only readback and
restart are verification steps. Users enter no IDs, fingerprints, nonces or SQL
and need no Inspector. This is an action inventory, not a measured human study.

## Focused effects and limitations

The existing test owner checks source-fingerprint drift and stale packet
refusal without writes, exact revision and Transition replay without duplicate
Core effects, exact execution replay without another adapter invocation,
post-execution revision refusal, historical payload preservation, and one final
canonical recovery validation. Existing P1/P1.5 scope, currentness, missing /
tampered source, exclusion, portability and revision-limit cases stay enabled.

The small scenario ends with exactly three packets, two receipts, three
proposals, one Decision, one gate, one semantic state and one Transition.
Read/preview/refusal paths compare database snapshots. Explicit authenticated
writers may also consume/rotate their existing local sessions. The Browser
effect ledger still requires exact table operations and source-bound effects;
its additional expected records are the correction, Decision, gate, semantic
state, Transition, later packet, next receipt and next assessment proposal.
No deletion is permitted, and immutable pre-existing rows remain unchanged.

There is no new retrieval, retention, withdrawal or deletion meaning. External
source availability/currentness and withdrawal permissions are not established
by a retained local note. Existing restrictions and recovery owners remain
unchanged. Rollback removes these test/report additions only; no data migration,
compatibility repair, runtime configuration change or source resealing is needed.

The chosen scenario creates no ContextUseReview and does not require the local
context-use probe. Issue #1221 remains separate: its probe receipt role mismatch
is neither fixed nor bypassed, and no probe records are deleted. The existing
Browser automation/probe case remains intact and is not used as the first or
next ordinary result in this new path.

## Cost boundaries and verification

One development observation of the file-backed scenario measured preparation
46.245 ms, first execution 165.071 ms, review 496.627 ms, successor writer
322.689 ms, fresh preparation 782.632 ms and next execution 1170.620 ms.
Each phase uses the same monotonic clock; writer timings exclude exact replay,
and review includes revision replay, Decision, preview and confirmation.
Fresh preparation includes database reopen, production read/admission and the
stale-packet negative check. These synthetic timings are not UI latency, model
latency or a comparison of alternatives.

The admitted packet has 23,771 Unicode code points and 23,771 UTF-8 bytes,
seven selected entries and an owner estimate of 5,917 tokens. The estimate is
not a measured tokenizer count or billing. There are exactly two deterministic
native requests, zero live provider calls and zero billed tokens. Returned
record counts and serialized bytes are not disk I/O; disk I/O and human burden
were not measured. Fresh connections prove application reconstruction, not
cold model-session isolation.

The native-host Browser focused command
`node scripts/run-canonical-test-suite.mjs e2e-operator-native-host-execution`
passed in 214.423 seconds, including all five workflow phases, exact effects,
diagnostics, bounded cleanup, zero remaining owned processes, and restoration
of the same live Companion. The new continuation phase took 51.337 seconds,
including its disposable service restart; this is not a human review duration.

The other focused commands passed: `npm run test:project-work-initialization`
(33.35 seconds), `npm run test:operator-execution-browser-fixture` (7.28 seconds),
`npm run test:operator-execution-result-contract` (0.19 seconds; 30 staged
finalization negatives) and `npm run test:operator-execution-effect-ledger`
(0.19 seconds; 12 equal-count/scope negatives). `git diff --check` passed.
Direct `tsc --noEmit --incremental false` found only generated
`.next/dev/types` route-export errors, with no changed-source diagnostics;
that failed check is preserved. The normal typecheck follows Local Canonical's
owned cleanup/regeneration of `.next`. The selected deciding plan and results
for the committed clean head are recorded in the PR closeout. No P1 receipt
transfers to this change.

Development failures are retained separately from deciding evidence: an
incorrect result-summary property, an unsupported test read argument, and a
synthetic ledger Transition missing its Decision binding were corrected in
test code. The first Browser attempt clicked a checkbox before it was enabled;
the test now waits for enabled controls and completed GuideBrief reads. The
second passed workflow phases but the final diagnostic guard rejected the
new phase's expected GuideBrief cancellation and the next project's existing
authentication bootstrap. Classification now covers only those existing exact
read/endpoint/status events in the added phase. The third passed. No runtime
guard, timeout, effect assertion, coverage, planner or receipt policy changed.

The first committed target, `d998cbc6ca1c011e9364044adbe4233e0db2f518`,
selected Full Canonical. Dependency preparation, normal typecheck and build
passed; unit verification correctly failed because the existing manifest
inventory test still expected 136 fields / 63 markers instead of the two new
fields and one new marker (138 / 64). Its 383.202-second failed run and
non-deciding receipt are preserved; remaining phases were skipped. Cleanup
completed with zero owned processes and the same live Companion restored.
The inventory assertion is corrected in a new commit, without dropping any
field, marker or assertion. The failed head is not retried; the new clean exact
head receives its own planner-selected deciding run.

## Separately authorized live execution card prerequisites

Live feasibility, live utility, model qualification and controlled comparisons
are **not_run**. A later card needs the exact task/artifact and information
cutoff; allowed data and egress destinations; verified host, model and config
including current runtime qualification/HOLD disposition; explicit call, run,
wall-time and monetary ceilings; stop conditions and whether any replacement
is authorized; and a named evaluator with predeclared acceptance criteria.
The evaluator must distinguish retained meaning from task success and account
for ambient memory, this agent's prior exposure and lack of cold-session
isolation. Nothing in these regressions authorizes that card, expands #1130 /
RW1B or CW1, or changes production credentials/configuration.
